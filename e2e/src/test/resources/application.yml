spring:
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:mysql://mysql.tool.net:3306/trade?autoReconnect=true&createDatabaseIfNotExist=true&characterEncoding=utf8&characterSetResults=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    dbcp2:
      test-on-borrow: true
      test-on-return: true
      validation-query: SELECT 1
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        default_catalog: trade
